import java.io.File;
import java.io.FileNotFoundException;

File file = new File( basedir, "target/dump.dsml" );
if ( !file.isFile() )
{
    throw new FileNotFoundException( "Could not DSML dump: " + file );
}